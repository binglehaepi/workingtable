# 뽀모도로 도트(Pixel) 에셋 가이드

todoary 뽀모도로 UI에 넣을 **픽셀 아트 토마토** 제작·납품 가이드입니다.  
에셋이 준비되면 `v2/timer.jsx`의 `PomodoroBarButton` 등에 `<img>` 또는 CSS 스프라이트로 연결합니다.

---

## 1. 만들 파일 (최소 세트)

| 파일명 | 크기 (1x) | 용도 |
|---|---|---|
| `tomato-bar-idle.png` | 32×40 | 할 일 탭 버튼 — 대기 (꼭지 포함) |
| `tomato-bar-active.png` | 56×32 | 할 일 탭 버튼 — 타이머 동작 중 (시간 텍스트는 코드에서 겹침) |
| `tomato-icon.png` | 16×16 | 팝업 타이틀바 아이콘 |

선택 (나중에):

| 파일명 | 용도 |
|---|---|
| `tomato-alert.png` | 집중/휴식 종료 알림 시 (표정·색 살짝 변경) |
| `tomato-break.png` | 휴식 단계 (민트 톤 등) |

**납품 경로:** `asset/pomo/`  
**@2x** (`64×80` 등)도 함께 주면 고해상도 디스플레이에서 선명합니다.

---

## 2. 캔버스·내보내기

- **격자:** 1px = 1도트 (Aseprite, Piskel, Pixaki 등)
- **형식:** PNG, **투명 배경**
- **안티앨리어싱:** 끄기 (nearest neighbor)
- **팔레트:** 앱 테마와 맞추기 (아래 색 참고)

---

## 3. 색 팔레트 (todoary 기본)

| 용도 | HEX | CSS 변수 |
|---|---|---|
| 외곽선·텍스트 | `#28333f` | `--ink` |
| 토마토 본체 (밝음) | `#ffb8cc` | `--pink-soft` |
| 토마토 본체 (중간) | `#ff7a9a` | — |
| 토마토 본체 (어둠) | `#ef6880` | — |
| 잎·꼭지 | `#9ed67a` | — |
| 줄기 | `#4d7a3e` | — |
| 하이라이트 | 왼쪽 위 1~2px 밝은 핑크 | — |

---

## 4. 비율·레이아웃

### 탭 버튼 (`tomato-bar-idle`)

- **가로:세로 ≈ 1 : 1.1** — 거의 원형, 살짝 세로로 긴 토마토
- **꼭지:** 위 중앙, 본체 너비의 약 60%
- **중앙 50%×40%:** 비워 두기 → 코드에서 `00:00` 표시

### 탭 버튼 — 타이머 중 (`tomato-bar-active`)

- **가로:세로 ≈ 1.4 : 1** — 가로로 살짝 넓은 타원
- 시간 5자 (`25:00`)가 들어갈 여백 확보

### 팝업 아이콘 (`tomato-icon`)

- 16×16 단순 실루엣, 꼭지 2~3px

---

## 5. 그릴 때 팁

1. **1px 아웃라인** (`#28333f`) — 앱 UI와 통일감
2. **세로축 대칭** — 작아도 귀여워 보임
3. **그림자** — 본체 아래 1px `#000` 15~20% (선택)
4. **스프라이트 시트** 대신 **파일별 PNG** 권장 (연결·교체 쉬움)

---

## 6. 연동 시 (개발자용)

에셋 추가 후:

1. `scripts/build-dist.mjs`의 `assets` 배열에 `asset` 폴더 포함 확인 (이미 있음)
2. `PomodoroBarButton` — `🍅` 이모지 → `<img src="asset/pomo/tomato-bar-idle.png" …>`
3. 타이머 중 — `tomato-bar-active.png` + `fmtMS(remaining)` 텍스트 오버레이
4. 알림 바운스 — 기존 `.pomo-dock-bounce` CSS 유지, 이미지만 교체

---

## 7. 체크리스트 (납품 전)

- [ ] PNG, 투명 배경
- [ ] 1x + @2x 세트
- [ ] `asset/pomo/` 경로에 배치
- [ ] 파일명 위 표와 일치
- [ ] 중앙 시간 영역 비워 둠 (active)

질문·시안 공유는 이 가이드와 함께 PR 또는 이슈에 첨부해 주세요.
