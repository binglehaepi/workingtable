# Todoary i18n Guide

이 문서는 다음에 언어를 추가하거나 번역 누락을 고칠 때 빠르게 따라가기 위한 가이드입니다.

## 핵심 파일

- `v2/i18n.jsx`
  - 모든 번역 사전과 언어 목록이 있습니다.
  - 기본 사전은 `DICT` 안에 있고, 추가 언어/보강 번역은 `EXTRA_DICT`, `UI_PATCH`로 합쳐집니다.
  - `LANGS`가 설정 탭에 표시되는 언어 버튼 목록입니다.
  - `WEEK`, `MONTHS`, `fmtDate()`가 요일/날짜 표시를 담당합니다.

- UI 파일
  - `v2/side-dock-v2.jsx`: 설정 탭의 언어 선택 UI, 사이드 탭
  - `v2/timer.jsx`: 상단 디데이
  - `v2/view-today.jsx`: 주간/하루 상세
  - `v2/view-todo.jsx`: 할 일, 반복, 마감 패널
  - `v2/view-memo.jsx`: 메모 에디터/툴바
  - `v2/view-mail.jsx`: 메일/프리셋
  - `v2/view-room.jsx`: 작업방

## 새 언어 추가 순서

1. `v2/i18n.jsx`의 `EXTRA_DICT`에 새 언어 코드를 추가합니다.
   - 예: `fr: { ... }`
   - 새 언어 사전은 `DICT.en` 위에 덮어쓰기 때문에, 빠진 키는 영어로 fallback됩니다.

2. `UI_PATCH`에도 같은 언어 코드를 추가합니다.
   - 최근에 발견된 버튼/툴팁/작업방/주간 보강 키들이 여기에 있습니다.
   - `EXTRA_DICT`만 추가하면 화면 일부가 영어 fallback으로 남을 수 있습니다.

3. `LANGS`에 설정 화면 표시 정보를 추가합니다.
   - 형식: `["fr", "Français", "FR"]`
   - 세 번째 값은 설정 탭 언어 버튼의 짧은 배지입니다.
   - 설정 탭에서는 현재 언어만 먼저 보이고, 클릭하면 전체 언어가 펼쳐지는 토글 UI를 사용합니다.

4. `WEEK`에 요일 약어를 추가합니다.
   - 일요일부터 토요일 순서입니다.
   - 형식: `fr: ["dim", "lun", "mar", "mer", "jeu", "ven", "sam"]`

5. 필요하면 `MONTHS`와 `fmtDate()`를 보강합니다.
   - 영어식 월 약어를 쓰지 않는 언어라면 `MONTHS`에 추가합니다.
   - 날짜 순서가 특수하면 `fmtDate()`에 분기를 추가합니다.

6. `detect()`에 브라우저 언어 감지를 추가합니다.
   - 예: `if (l.startsWith("fr")) return "fr";`

## 중국어 코드 규칙

- 중국어는 간체/번체를 분리합니다.
  - `zh-Hans`: 简体中文
  - `zh-Hant`: 繁體中文
- 예전 저장값 `zh`는 호환을 위해 `zh-Hans`로 정규화합니다.
- 브라우저 언어가 `zh-TW`, `zh-HK`, `zh-MO`, `Hant` 계열이면 `zh-Hant`로 감지합니다.
- 그 외 `zh` 계열은 `zh-Hans`로 감지합니다.

## 번역 키 작성 규칙

- 화면 문자열은 직접 하드코딩하지 말고 `L("key")`를 사용합니다.
- 변수는 `{name}` 형태로 둡니다.
  - 예: `"room.codeCopied": "{code} copied"`
  - 사용: `L("room.codeCopied", { code })`
- 줄바꿈이 필요한 문구는 문자열 안에 `\n`을 명시합니다.
  - 예: `"room.emptyHint": "Line one.\nLine two."`
- 좁은 UI 버튼은 짧은 번역을 우선합니다.
  - 설정 탭 언어 버튼처럼 긴 원어명은 말줄임 처리됩니다.
- 언어가 많아졌기 때문에 설정 탭 언어 목록은 접는 토글 형태를 유지합니다.

## 번역 누락 찾기

사용자에게 보이는 하드코딩 문자열을 찾을 때는 다음 패턴을 검색합니다.

```powershell
rg 'title=\"|placeholder=\"|>[^<{]*[가-힣ぁ-んァ-ン一-龥]|\"[^\"]*[가-힣][^\"]*\"' v2 -g '*.jsx'
```

결과에서 주석은 제외하고, 실제 JSX 텍스트/`title`/`placeholder`/알림 문구를 우선 고칩니다.

## 자주 놓치는 곳

- `title="..."` 툴팁
- `placeholder="..."`
- 버튼 본문 텍스트
- 토스트/flash 메시지
- 저장된 기본값이 화면에 그대로 나오는 경우
  - 예: `dday.label === "디데이"`이면 표시할 때 `L("dday.label")`로 보정
- 날짜/시간 포맷
  - `오전/오후`, 요일명, `월/일` 순서 등
- 삽입되는 HTML 문자열
  - 예: 메모 에디터의 체크박스 블록 텍스트

## 간단 검증 체크리스트

- 설정 탭에서 새 언어 버튼이 보이는가?
- 탭 이름, 상단 디데이, 주간 오늘 배지, 작업방 설명이 바뀌는가?
- 할 일 반복/마감 패널 버튼이 바뀌는가?
- 메모 에디터 툴바와 메일 툴팁이 바뀌는가?
- `rg` 검색에서 사용자 노출 문자열이 남지 않았는가?
